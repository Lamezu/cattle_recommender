services:
  neo4j:
    image: neo4j:latest
    container_name: cattle_neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/password
    volumes:
      - neo4j_data:/data
      - ./data:/var/lib/neo4j/import

volumes:
  neo4j_data:
